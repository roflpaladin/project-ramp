node_modules/
.next/
out/
.env
.env.local
.env*.local
.vercel
.DS_Store
*.tsbuildinfo
next-env.d.ts
# Written by `npm run test:coverage` (vitest v8 provider).
coverage/
# Playwright local-run artifacts (Ticket 29 e2e specs are local-run, never a CI job).
test-results/
playwright-report/

# Internal working docs — keep out of the public repo
Project Ramp/
.claude/
/ramp-brand-guideline.html
# Local ECC tooling checkout. Untracked, but tsconfig's "**/*.ts" include still
# sweeps it into `npm run typecheck`, which fails on its sample assets. CI never
# sees it (fresh checkout), so ignoring it here keeps the local invariant usable.
ECC/
# Sprint construction plans — working artifacts. Sprint truth lives in Notion.
# The leading slash matters: a bare "plans/" matches at ANY depth and silently
# swallowed lib/plans/ and tests/plans/ when the plan data layer landed
# (Ticket 24). Anchored to the repo root only.
/plans/
